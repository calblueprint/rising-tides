<%= form_with(model: community_leader, local: true) do |form| %>
  <% if community_leader.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(community_leader.errors.count, "error") %> prohibited this community_leader from being saved:</h2>

      <ul>
      <% community_leader.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
