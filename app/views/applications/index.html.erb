<% if user_signed_in? || organization_signed_in?  %>
    <%= react_component('projects/volunteers/ApplicationsIndex', { applications: @applications, user: current_user, organization: current_organization }) %>
<% end %>

