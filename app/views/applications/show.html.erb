<% if user_signed_in? %>
    <%= react_component('utils/ApplicationReview', { application: @application, user: current_user, project: @application.project, organization: @application.project.organization}) %>
<% elsif organization_signed_in? %>
    <%= react_component('projects/organizations/ApplicationView', { application: @application, user: @user }) %>
<% end %>

