<% if user_signed_in? %>
    <%= react_component('utils/ApplicationReview', { application: @application, user: current_user profile_image_url: @profile_image_url, resume_url: @resume_url}) %>
<% elsif organization_signed_in? %>
    <%= react_component('projects/organizations/ApplicationView', { application: @application, user: @user }) %>
<% end %>

