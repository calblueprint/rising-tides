<% if user_signed_in? %>
    <%= react_component('projects/volunteers/ProjectView', { project: @project, photos: @photos }) %>
<% elsif organization_signed_in? %>
    <%= react_component('projects/organizations/ProjectView', { project: @project, photos: @photos }) %>
<% end %>

