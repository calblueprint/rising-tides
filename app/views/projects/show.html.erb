<% if user_signed_in? || organization_signed_in? %>
    <%= react_component('projects/ProjectView', { project: @project, photos: @photos, project_type: @project_type, organization: @project.organization, org_image_url: @org_image_url, organization_signed_in: organization_signed_in?, current_organization: current_organization, applications: @applications }) %>
<% end %>
